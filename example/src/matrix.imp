fn makevector(u32 ub) -> u32[ub] {
    lb = [0];
    res = { 1 + iv[0] + ub | lb <= iv < [ub] };
    return res;
}

fn makematrix(u32 cols, u32 rows) -> u32[cols,rows] {
    res = { { iv[0] + jv[0] | [0] <= jv < [cols] }
          | [0] <= iv < [rows] };
    return res;
}
