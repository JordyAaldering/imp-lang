[package]
name = "stdlib"
version = "0.1.0"
edition = "2024"

[dependencies]
imp_core = { path = "../imp_core" }

[build-dependencies]
cc = "1.4.4"
imp_lang = { path = "../imp_lang" }

[dev-dependencies]
parameterized = "2.1.0"
